git status

git checkout -b myBranchName

git push
:: then the copy and paste the real command, for example:
git push --set-upstream origin git_commands

git pull 
:: all day long

git add .
git add --patch

:: bunch of question answering

git commit -m "commit message"

git push

:: sometimes do this to make sure main branch code is merged into your current local branch
git merge main 

git switch main
git pull

git branch -D old_branch_name
git push origin --delete feature/login
